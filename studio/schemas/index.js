// Import all schema files
import siteSettings from "./siteSettings";
import blogPost from "./blogPost";
import category from "./category";

// Export all schemas
export const schemaTypes = [siteSettings, blogPost, category];
