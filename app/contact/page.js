import ContactContent from "./ContactContent";

export const metadata = {
   title: "Contact | Gayan de Silva, PhD",
   description:
      "Book a 15-minute AI Audit discovery call with Gayan de Silva, PhD. Hamburg, Prague, Europe-wide, remote engagements.",
};

export default function ContactPage() {
   return <ContactContent />;
}
